import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
    AdminProduct: {
        padding: '80px',
        '& .box-table': {
            width: 'fit-content'
        }
    }
}));



